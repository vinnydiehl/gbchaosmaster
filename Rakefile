#!/usr/bin/env rake

# To add a task, place it in a file in lib/tasks ending in .rake and it will
# automatically be included.

require File.expand_path('../config/application', __FILE__)

Gbchaosmaster::Application.load_tasks
